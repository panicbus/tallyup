// Business-logic services land here starting W2 (e.g. check-in confirmation,
// redemption rollover rules). Routes call into services; services call into
// ../data-access. No HTTP or DB concerns belong in this layer.
export {};
